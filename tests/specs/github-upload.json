[
  {
    "name": "Upload to GitHub shows error when no project",
    "steps": [
      {
        "send": { "callback": "github:upload" },
        "expect": [
          {
            "method": "answerCallbackQuery"
          },
          {
            "method": "sendMessage",
            "payload": {
              "text": "No project to upload. Tap 📝 Generate code first."
            }
          }
        ]
      }
    ]
  }
]
